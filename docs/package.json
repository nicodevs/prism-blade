{
  "name": "prism-demo",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "node generate-index.mjs && vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.0.4"
  },
  "dependencies": {
    "@nicodevs/prism-themes": "^0.0.4",
    "marked": "^16.0.0",
    "prism-blade": "^0.0.5",
    "prismjs": "^1.30.0"
  }
}
